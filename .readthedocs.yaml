version: 2

sphinx:
  configuration: docs/source/conf.py
  builder: html
  build_dir: docs

python:
  version: 3.8
  install:
    - method: pip
      path: .
      extra_requirements:
        - docs

build:
  image: latest
